#path that are constants
#Its a general naming convention in python to write the constant names in all capital letters

from pathlib import Path

CONFIG_FILE_PATH = Path("config/config.yaml")
PARAMS_FILE_PATH = Path("params.yaml")
